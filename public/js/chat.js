"use strict"

formularioId.value = nevera.innerHTML;

console.log(nevera.innerHTML);

if (impresora.innerHTML === 0) {
    datosUsuario.classList.add("ocultar")
} else {
    datosUsuario.classList.remove("ocultar")
}